# ============================================================================
# IU NWEO AI — Python Dependencies
# Maintainer: Architect
# ============================================================================
# Phase 1: Core + Streaming
# Phase 2: LangGraph + Checkpointer (installed now, used in Phase 2)
# Phase 3: RAG stores (installed now, used in Phase 3)
# Phase 4: Semantic Router (installed now, used in Phase 4)
# ============================================================================

# --- Core Framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
sse-starlette>=2.2.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0
httpx>=0.28.0

# --- LangChain Core ---
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0

# --- LLM: Gemini ---
langchain-google-genai>=2.1.0
google-generativeai>=0.8.0

# --- LangGraph: Orchestration Engine ---
langgraph>=0.4.0
langgraph-checkpoint-postgres>=2.0.0

# --- PostgreSQL Driver (psycopg v3, NOT psycopg2) ---
psycopg[binary,pool]>=3.2.0

# --- ChromaDB: Vector Store ---
chromadb>=1.5.0
langchain-chroma>=0.2.0

# --- Neo4j: Knowledge Graph ---
neo4j>=5.27.0
langchain-neo4j>=0.3.0

# --- Semantic Router: Fast Intent Classification ---
semantic-router>=0.1.0

# --- Utilities ---
tenacity>=9.0.0
